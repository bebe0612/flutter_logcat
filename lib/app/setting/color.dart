import 'package:flutter/material.dart';

class AppColor {
  static dashboardBackground(BuildContext context) =>
      Theme.of(context).backgroundColor;
  static main(BuildContext context) => Theme.of(context).primaryColor;
}
